# SMTP secret file (optional)

If you set `SMTP_PASSWORD_FILE=/usr/src/app/secrets/smtp_password.txt` in `.env`, put the **mailbox password** in a single-line file here on the server (not committed to git). See `docs-archive/EMAIL_SETUP.md`.
